# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @work-pack/work-auth`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t work-pack-work-auth`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system work-pack-work-auth && \
          adduser --system -G work-pack-work-auth work-pack-work-auth

COPY apps/work-auth/dist work-pack-work-auth/
COPY apps/work-auth/package.json work-pack-work-auth/
RUN chown -R work-pack-work-auth:work-pack-work-auth .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix work-pack-work-auth --omit=dev -f install

CMD [ "node", "work-pack-work-auth" ]
